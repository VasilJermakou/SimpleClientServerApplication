# SimpleClientServerApplication
Simple Java project. The client side sends a request (type of mathematical operation, operands), the server side processes the request and generates a response to the client.
